

%%% perl code : polynomial_substitute_rational_number.pl

\setcounter{quesnum}{10}

\begin{multicols}{2}
\begin{enumerate}
  \itemsep=90pt
  \item $\displaystyle f(x)=\left( x+2\right)^{2}-9$

$f(4)$
        \phantom{$=27$}

  \item $\displaystyle f(x)=-2\left( x-\frac{5}{4}\right)^{2}-\frac{15}{8}$

$f(-5)$
        \phantom{$=-80$}

  \item $\displaystyle f(x)=-5\left( x-\frac{1}{5}\right)^{2}-\frac{9}{5}$

$f(-2)$
        \phantom{$=-26$}

  \item $\displaystyle f(x)=4\left( x-\frac{3}{8}\right)^{2}-\frac{9}{16}$

$f(-3)$
        \phantom{$=45$}

  \item $\displaystyle f(x)=-5\left( x+\frac{1}{2}\right)^{2}-\frac{15}{4}$

$f(5)$
        \phantom{$=-155$}

\columnbreak % 5

  \item $\displaystyle f(x)=-5\left( x+\frac{1}{2}\right)^{2}+\frac{9}{4}$

$f(-3)$
        \phantom{$=-29$}

  \item $\displaystyle f(x)=-3\left( x-\frac{2}{3}\right)^{2}+\frac{1}{3}$

$f(3)$
        \phantom{$=-16$}

  \item $\displaystyle f(x)=\left( x+2\right)^{2}-1$

$f(1)$
        \phantom{$=8$}

  \item $\displaystyle f(x)=3\left( x+\frac{1}{2}\right)^{2}+\frac{13}{4}$

$f(4)$
        \phantom{$=64$}

  \item $\displaystyle f(x)=-2\left( x-\frac{3}{4}\right)^{2}+\frac{9}{8}$

$f(3)$
        \phantom{$=-9$}

\columnbreak % 10

\resetques

  \item $\displaystyle f(x)=\left( x-\frac{3}{2}\right)^{2}+\frac{3}{4}$

$f(2)$
        \phantom{$=1$}

  \item $\displaystyle f(x)=4\left( x+\frac{1}{2}\right)^{2}+4$

$f(4)$
        \phantom{$=85$}

  \item $\displaystyle f(x)=-3\left( x+\frac{2}{3}\right)^{2}+\frac{4}{3}$

$f(-2)$
        \phantom{$=-4$}

  \item $\displaystyle f(x)=-4\left( x-\frac{1}{2}\right)^{2}+6$

$f(-5)$
        \phantom{$=-115$}

  \item $\displaystyle f(x)=-3\left( x-\frac{1}{2}\right)^{2}+\frac{11}{4}$

$f(0)$
        \phantom{$=2$}

\columnbreak % 15

  \item $\displaystyle f(x)=\left( x+\frac{1}{2}\right)^{2}-\frac{21}{4}$

$f(-2)$
        \phantom{$=-3$}

  \item $\displaystyle f(x)=\left( x-\frac{1}{2}\right)^{2}-\frac{17}{4}$

$f(-1)$
        \phantom{$=-2$}

  \item $\displaystyle f(x)=-\left( x+2\right)^{2}+7$

$f(4)$
        \phantom{$=-29$}

  \item $\displaystyle f(x)=3\left( x-\frac{1}{3}\right)^{2}+\frac{5}{3}$

$f(0)$
        \phantom{$=2$}

  \item $\displaystyle f(x)=2\left( x-1\right)^{2}-1$

$f(-5)$
        \phantom{$=71$}

\columnbreak % 20

\resetques

  \item $\displaystyle f(x)=3\left( x+\frac{1}{3}\right)^{2}-\frac{10}{3}$

$f(-3)$
        \phantom{$=18$}

  \item $\displaystyle f(x)=5\left( x-\frac{1}{2}\right)^{2}+\frac{7}{4}$

$f(-5)$
        \phantom{$=153$}

  \item $\displaystyle f(x)=-\left( x-\frac{5}{2}\right)^{2}+\frac{37}{4}$

$f(1)$
        \phantom{$=7$}

  \item $\displaystyle f(x)=2\left( x+\frac{1}{4}\right)^{2}+\frac{23}{8}$

$f(3)$
        \phantom{$=24$}

  \item $\displaystyle f(x)=-2\left( x-\frac{5}{4}\right)^{2}-\frac{7}{8}$

$f(2)$
        \phantom{$=-2$}

\columnbreak % 25

  \item $\displaystyle f(x)=-5\left( x+\frac{1}{10}\right)^{2}-\frac{99}{20}$

$f(-2)$
        \phantom{$=-23$}

  \item $\displaystyle f(x)=\left( x-1\right)^{2}+3$

$f(-3)$
        \phantom{$=19$}

  \item $\displaystyle f(x)=-2\left( x+\frac{3}{4}\right)^{2}+\frac{9}{8}$

$f(1)$
        \phantom{$=-5$}

  \item $\displaystyle f(x)=-4\left( x+\frac{1}{2}\right)^{2}-3$

$f(2)$
        \phantom{$=-28$}

  \item $\displaystyle f(x)=3\left( x-\frac{2}{3}\right)^{2}-\frac{10}{3}$

$f(4)$
        \phantom{$=30$}

\columnbreak % 30

\resetques

  \item $\displaystyle f(x)=-4\left( x+\frac{3}{8}\right)^{2}-\frac{71}{16}$

$f(-3)$
        \phantom{$=-32$}

  \item $\displaystyle f(x)=5\left( x-\frac{2}{5}\right)^{2}-\frac{19}{5}$

$f(-2)$
        \phantom{$=25$}

  \item $\displaystyle f(x)=-3\left( x-\frac{1}{3}\right)^{2}+\frac{7}{3}$

$f(-1)$
        \phantom{$=-3$}

  \item $\displaystyle f(x)=-\left( x-1\right)^{2}$

$f(0)$
        \phantom{$=-1$}

  \item $\displaystyle f(x)=-4\left( x-\frac{1}{2}\right)^{2}+6$

$f(-4)$
        \phantom{$=-75$}

\columnbreak % 35

  \item $\displaystyle f(x)=\left( x-\frac{3}{2}\right)^{2}+\frac{11}{4}$

$f(-4)$
        \phantom{$=33$}

  \item $\displaystyle f(x)=3\left( x-\frac{5}{6}\right)^{2}+\frac{23}{12}$

$f(5)$
        \phantom{$=54$}

  \item $\displaystyle f(x)=\left( x+2\right)^{2}+1$

$f(3)$
        \phantom{$=26$}

  \item $\displaystyle f(x)=-4\left( x+\frac{1}{4}\right)^{2}-\frac{19}{4}$

$f(5)$
        \phantom{$=-115$}

  \item $\displaystyle f(x)=2\left( x-\frac{5}{4}\right)^{2}-\frac{49}{8}$

$f(5)$
        \phantom{$=22$}

\columnbreak % 40

\resetques

  \item $\displaystyle f(x)=-3\left( x+\frac{5}{6}\right)^{2}-\frac{23}{12}$

$f(4)$
        \phantom{$=-72$}

  \item $\displaystyle f(x)=-4\left( x+\frac{1}{2}\right)^{2}+4$

$f(-1)$
        \phantom{$=3$}

  \item $\displaystyle f(x)=-2\left( x-\frac{1}{2}\right)^{2}-\frac{1}{2}$

$f(-4)$
        \phantom{$=-41$}

  \item $\displaystyle f(x)=\left( x-\frac{5}{2}\right)^{2}-\frac{13}{4}$

$f(3)$
        \phantom{$=-3$}

  \item $\displaystyle f(x)=5\left( x-\frac{2}{5}\right)^{2}-\frac{14}{5}$

$f(0)$
        \phantom{$=-2$}

\columnbreak % 45

  \item $\displaystyle f(x)=3\left( x+\frac{2}{3}\right)^{2}-\frac{1}{3}$

$f(5)$
        \phantom{$=96$}

  \item $\displaystyle f(x)=-3\left( x+\frac{5}{6}\right)^{2}+\frac{73}{12}$

$f(0)$
        \phantom{$=4$}

  \item $\displaystyle f(x)=2\left( x-1\right)^{2}+1$

$f(-3)$
        \phantom{$=33$}

  \item $\displaystyle f(x)=-5\left( x+\frac{1}{2}\right)^{2}+\frac{9}{4}$

$f(-4)$
        \phantom{$=-59$}

  \item $\displaystyle f(x)=-5\left( x+\frac{2}{5}\right)^{2}-\frac{11}{5}$

$f(0)$
        \phantom{$=-3$}

\columnbreak % 50

\resetques

  \item $\displaystyle f(x)=-3\left( x+\frac{1}{6}\right)^{2}-\frac{23}{12}$

$f(-1)$
        \phantom{$=-4$}

  \item $\displaystyle f(x)=-\left( x-\frac{5}{2}\right)^{2}+\frac{9}{4}$

$f(5)$
        \phantom{$=-4$}

  \item $\displaystyle f(x)=2\left( x-1\right)^{2}-7$

$f(-3)$
        \phantom{$=25$}

  \item $\displaystyle f(x)=4\left( x-\frac{1}{2}\right)^{2}+4$

$f(0)$
        \phantom{$=5$}

  \item $\displaystyle f(x)=-3\left( x-\frac{1}{6}\right)^{2}-\frac{35}{12}$

$f(2)$
        \phantom{$=-13$}

\columnbreak % 55

  \item $\displaystyle f(x)=4\left( x+\frac{1}{8}\right)^{2}+\frac{47}{16}$

$f(4)$
        \phantom{$=71$}

  \item $\displaystyle f(x)=-5\left( x-\frac{3}{10}\right)^{2}+\frac{69}{20}$

$f(-3)$
        \phantom{$=-51$}

  \item $\displaystyle f(x)=5\left( x-\frac{2}{5}\right)^{2}-\frac{19}{5}$

$f(-3)$
        \phantom{$=54$}

  \item $\displaystyle f(x)=-5\left( x-\frac{1}{10}\right)^{2}-\frac{39}{20}$

$f(-3)$
        \phantom{$=-50$}

  \item $\displaystyle f(x)=5\left( x+\frac{1}{2}\right)^{2}-\frac{9}{4}$

$f(4)$
        \phantom{$=99$}

\columnbreak % 60

\resetques

  \item $\displaystyle f(x)=-3\left( x-\frac{1}{2}\right)^{2}+\frac{3}{4}$

$f(-2)$
        \phantom{$=-18$}

  \item $\displaystyle f(x)=4\left( x+\frac{3}{8}\right)^{2}-\frac{89}{16}$

$f(4)$
        \phantom{$=71$}

  \item $\displaystyle f(x)=-4\left( x-\frac{1}{8}\right)^{2}+\frac{1}{16}$

$f(1)$
        \phantom{$=-3$}

  \item $\displaystyle f(x)=5\left( x+\frac{1}{2}\right)^{2}+\frac{15}{4}$

$f(-3)$
        \phantom{$=35$}

  \item $\displaystyle f(x)=5\left( x+\frac{1}{10}\right)^{2}-\frac{61}{20}$

$f(-3)$
        \phantom{$=39$}

\columnbreak % 65

  \item $\displaystyle f(x)=5\left( x+\frac{1}{10}\right)^{2}+\frac{59}{20}$

$f(5)$
        \phantom{$=133$}

  \item $\displaystyle f(x)=5\left( x+\frac{1}{5}\right)^{2}+\frac{14}{5}$

$f(2)$
        \phantom{$=27$}

  \item $\displaystyle f(x)=4\left( x+\frac{1}{4}\right)^{2}+\frac{3}{4}$

$f(2)$
        \phantom{$=21$}

  \item $\displaystyle f(x)=-2\left( x+\frac{1}{2}\right)^{2}+\frac{7}{2}$

$f(3)$
        \phantom{$=-21$}

  \item $\displaystyle f(x)=-5\left( x+\frac{2}{5}\right)^{2}+\frac{4}{5}$

$f(-3)$
        \phantom{$=-33$}

\columnbreak % 70

\resetques

  \item $\displaystyle f(x)=\left( x-1\right)^{2}+2$

$f(5)$
        \phantom{$=18$}

  \item $\displaystyle f(x)=-3\left( x+\frac{1}{2}\right)^{2}+\frac{3}{4}$

$f(-1)$
        \phantom{$=0$}

  \item $\displaystyle f(x)=3\left( x+\frac{2}{3}\right)^{2}+\frac{8}{3}$

$f(0)$
        \phantom{$=4$}

  \item $\displaystyle f(x)=3\left( x-\frac{2}{3}\right)^{2}-\frac{10}{3}$

$f(-3)$
        \phantom{$=37$}

  \item $\displaystyle f(x)=-4\left( x-\frac{1}{2}\right)^{2}+4$

$f(-4)$
        \phantom{$=-77$}

\columnbreak % 75

  \item $\displaystyle f(x)=-\left( x+1\right)^{2}+1$

$f(0)$
        \phantom{$=0$}

  \item $\displaystyle f(x)=-5\left( x-\frac{2}{5}\right)^{2}-\frac{1}{5}$

$f(4)$
        \phantom{$=-65$}

  \item $\displaystyle f(x)=4\left( x+\frac{1}{2}\right)^{2}$

$f(-4)$
        \phantom{$=49$}

  \item $\displaystyle f(x)=-3\left( x-\frac{1}{6}\right)^{2}+\frac{49}{12}$

$f(-2)$
        \phantom{$=-10$}

  \item $\displaystyle f(x)=2\left( x+\frac{1}{2}\right)^{2}-\frac{5}{2}$

$f(3)$
        \phantom{$=22$}

\columnbreak % 80

\resetques

  \item $\displaystyle f(x)=2\left( x+\frac{1}{4}\right)^{2}-\frac{41}{8}$

$f(4)$
        \phantom{$=31$}

  \item $\displaystyle f(x)=3\left( x+\frac{2}{3}\right)^{2}+\frac{8}{3}$

$f(4)$
        \phantom{$=68$}

  \item $\displaystyle f(x)=-5\left( x+\frac{2}{5}\right)^{2}+\frac{14}{5}$

$f(4)$
        \phantom{$=-94$}

  \item $\displaystyle f(x)=\left( x-1\right)^{2}+1$

$f(-5)$
        \phantom{$=37$}

  \item $\displaystyle f(x)=4\left( x+\frac{1}{4}\right)^{2}+\frac{3}{4}$

$f(1)$
        \phantom{$=7$}

\columnbreak % 85

  \item $\displaystyle f(x)=-3\left( x-\frac{5}{6}\right)^{2}-\frac{11}{12}$

$f(-1)$
        \phantom{$=-11$}

  \item $\displaystyle f(x)=-2\left( x+\frac{5}{4}\right)^{2}+\frac{25}{8}$

$f(-2)$
        \phantom{$=2$}

  \item $\displaystyle f(x)=-4\left( x+\frac{1}{8}\right)^{2}-\frac{63}{16}$

$f(2)$
        \phantom{$=-22$}

  \item $\displaystyle f(x)=-4\left( x+\frac{1}{8}\right)^{2}+\frac{17}{16}$

$f(-4)$
        \phantom{$=-59$}

  \item $\displaystyle f(x)=-4\left( x+\frac{1}{2}\right)^{2}-3$

$f(0)$
        \phantom{$=-4$}

\columnbreak % 90

\resetques

  \item $\displaystyle f(x)=-3\left( x+\frac{1}{2}\right)^{2}+\frac{7}{4}$

$f(-1)$
        \phantom{$=1$}

  \item $\displaystyle f(x)=-3\left( x+\frac{2}{3}\right)^{2}+\frac{10}{3}$

$f(0)$
        \phantom{$=2$}

  \item $\displaystyle f(x)=3\left( x-\frac{1}{2}\right)^{2}+\frac{1}{4}$

$f(-4)$
        \phantom{$=61$}

  \item $\displaystyle f(x)=-5\left( x-\frac{3}{10}\right)^{2}+\frac{109}{20}$

$f(2)$
        \phantom{$=-9$}

  \item $\displaystyle f(x)=-3\left( x+\frac{5}{6}\right)^{2}+\frac{61}{12}$

$f(-1)$
        \phantom{$=5$}

\columnbreak % 95

  \item $\displaystyle f(x)=-4\left( x-\frac{3}{8}\right)^{2}-\frac{55}{16}$

$f(-2)$
        \phantom{$=-26$}

  \item $\displaystyle f(x)=\left( x-2\right)^{2}-1$

$f(-3)$
        \phantom{$=24$}

  \item $\displaystyle f(x)=\left( x-\frac{3}{2}\right)^{2}-\frac{13}{4}$

$f(-1)$
        \phantom{$=3$}

  \item $\displaystyle f(x)=5\left( x+\frac{1}{10}\right)^{2}+\frac{59}{20}$

$f(3)$
        \phantom{$=51$}

  \item $\displaystyle f(x)=5\left( x+\frac{1}{2}\right)^{2}-\frac{9}{4}$

$f(-4)$
        \phantom{$=59$}

\columnbreak % 100

\resetques

  \item $\displaystyle f(x)=4\left( x+\frac{5}{8}\right)^{2}-\frac{89}{16}$

$f(4)$
        \phantom{$=80$}

  \item $\displaystyle f(x)=-2\left( x-\frac{5}{4}\right)^{2}-\frac{7}{8}$

$f(-3)$
        \phantom{$=-37$}

  \item $\displaystyle f(x)=-5\left( x-\frac{1}{2}\right)^{2}-\frac{3}{4}$

$f(5)$
        \phantom{$=-102$}

  \item $\displaystyle f(x)=3\left( x+\frac{1}{6}\right)^{2}-\frac{25}{12}$

$f(-3)$
        \phantom{$=22$}

  \item $\displaystyle f(x)=3\left( x+\frac{2}{3}\right)^{2}-\frac{4}{3}$

$f(5)$
        \phantom{$=95$}

\columnbreak % 105

  \item $\displaystyle f(x)=5\left( x-\frac{1}{10}\right)^{2}-\frac{61}{20}$

$f(-5)$
        \phantom{$=127$}

  \item $\displaystyle f(x)=-2\left( x-1\right)^{2}-2$

$f(0)$
        \phantom{$=-4$}

  \item $\displaystyle f(x)=3\left( x+\frac{1}{6}\right)^{2}+\frac{23}{12}$

$f(5)$
        \phantom{$=82$}

  \item $\displaystyle f(x)=-2\left( x+\frac{5}{4}\right)^{2}+\frac{65}{8}$

$f(-1)$
        \phantom{$=8$}

  \item $\displaystyle f(x)=-5\left( x+\frac{3}{10}\right)^{2}+\frac{69}{20}$

$f(-2)$
        \phantom{$=-11$}

\columnbreak % 110

\resetques

  \item $\displaystyle f(x)=\left( x+\frac{3}{2}\right)^{2}-\frac{9}{4}$

$f(5)$
        \phantom{$=40$}

  \item $\displaystyle f(x)=-2\left( x+\frac{3}{4}\right)^{2}+\frac{33}{8}$

$f(-4)$
        \phantom{$=-17$}

  \item $\displaystyle f(x)=-\left( x+\frac{3}{2}\right)^{2}+\frac{1}{4}$

$f(-4)$
        \phantom{$=-6$}

  \item $\displaystyle f(x)=-5\left( x+\frac{2}{5}\right)^{2}-\frac{16}{5}$

$f(5)$
        \phantom{$=-149$}

  \item $\displaystyle f(x)=-2\left( x-\frac{1}{2}\right)^{2}+\frac{9}{2}$

$f(-3)$
        \phantom{$=-20$}

\columnbreak % 115

  \item $\displaystyle f(x)=-3\left( x-\frac{1}{3}\right)^{2}-\frac{5}{3}$

$f(1)$
        \phantom{$=-3$}

  \item $\displaystyle f(x)=5\left( x+\frac{1}{5}\right)^{2}+\frac{4}{5}$

$f(4)$
        \phantom{$=89$}

  \item $\displaystyle f(x)=2\left( x+\frac{1}{4}\right)^{2}+\frac{31}{8}$

$f(-3)$
        \phantom{$=19$}

  \item $\displaystyle f(x)=-2\left( x+1\right)^{2}+5$

$f(5)$
        \phantom{$=-67$}

  \item $\displaystyle f(x)=-4\left( x+\frac{1}{4}\right)^{2}-\frac{19}{4}$

$f(2)$
        \phantom{$=-25$}

\columnbreak % 120

\resetques

  \item $\displaystyle f(x)=\left( x+2\right)^{2}-3$

$f(2)$
        \phantom{$=13$}

  \item $\displaystyle f(x)=3\left( x+\frac{5}{6}\right)^{2}-\frac{37}{12}$

$f(5)$
        \phantom{$=99$}

  \item $\displaystyle f(x)=-2\left( x+\frac{5}{4}\right)^{2}+\frac{65}{8}$

$f(-1)$
        \phantom{$=8$}

  \item $\displaystyle f(x)=2\left( x+\frac{1}{2}\right)^{2}-\frac{1}{2}$

$f(3)$
        \phantom{$=24$}

  \item $\displaystyle f(x)=3\left( x+\frac{5}{6}\right)^{2}-\frac{85}{12}$

$f(-5)$
        \phantom{$=45$}

\columnbreak % 125

  \item $\displaystyle f(x)=3\left( x+\frac{1}{2}\right)^{2}+\frac{9}{4}$

$f(1)$
        \phantom{$=9$}

  \item $\displaystyle f(x)=2\left( x+1\right)^{2}-6$

$f(3)$
        \phantom{$=26$}

  \item $\displaystyle f(x)=-4\left( x-\frac{3}{8}\right)^{2}+\frac{73}{16}$

$f(5)$
        \phantom{$=-81$}

  \item $\displaystyle f(x)=-2\left( x+\frac{1}{4}\right)^{2}-\frac{7}{8}$

$f(-4)$
        \phantom{$=-29$}

  \item $\displaystyle f(x)=5\left( x-\frac{1}{2}\right)^{2}-\frac{17}{4}$

$f(5)$
        \phantom{$=97$}

\columnbreak % 130

\resetques

  \item $\displaystyle f(x)=4\left( x+\frac{3}{8}\right)^{2}-\frac{89}{16}$

$f(1)$
        \phantom{$=2$}

  \item $\displaystyle f(x)=-\left( x+1\right)^{2}+1$

$f(-3)$
        \phantom{$=-3$}

  \item $\displaystyle f(x)=-5\left( x-\frac{1}{5}\right)^{2}+\frac{21}{5}$

$f(-3)$
        \phantom{$=-47$}

  \item $\displaystyle f(x)=2\left( x-1\right)^{2}-4$

$f(-3)$
        \phantom{$=28$}

  \item $\displaystyle f(x)=-2\left( x+\frac{1}{4}\right)^{2}-\frac{7}{8}$

$f(-2)$
        \phantom{$=-7$}

\columnbreak % 135

  \item $\displaystyle f(x)=-3\left( x-\frac{5}{6}\right)^{2}+\frac{73}{12}$

$f(-3)$
        \phantom{$=-38$}

  \item $\displaystyle f(x)=-3\left( x-\frac{5}{6}\right)^{2}+\frac{25}{12}$

$f(4)$
        \phantom{$=-28$}

  \item $\displaystyle f(x)=3\left( x+\frac{1}{3}\right)^{2}+\frac{14}{3}$

$f(4)$
        \phantom{$=61$}

  \item $\displaystyle f(x)=-5\left( x-\frac{1}{5}\right)^{2}-\frac{19}{5}$

$f(5)$
        \phantom{$=-119$}

  \item $\displaystyle f(x)=4\left( x+\frac{1}{4}\right)^{2}-\frac{9}{4}$

$f(5)$
        \phantom{$=108$}

\columnbreak % 140

\resetques

  \item $\displaystyle f(x)=\left( x-\frac{1}{2}\right)^{2}+\frac{19}{4}$

$f(0)$
        \phantom{$=5$}

  \item $\displaystyle f(x)=\left( x+\frac{1}{2}\right)^{2}-\frac{1}{4}$

$f(-5)$
        \phantom{$=20$}

  \item $\displaystyle f(x)=3\left( x+\frac{2}{3}\right)^{2}+\frac{5}{3}$

$f(1)$
        \phantom{$=10$}

  \item $\displaystyle f(x)=4\left( x+\frac{1}{2}\right)^{2}-2$

$f(-4)$
        \phantom{$=47$}

  \item $\displaystyle f(x)=4\left( x-\frac{5}{8}\right)^{2}-\frac{41}{16}$

$f(-2)$
        \phantom{$=25$}

\columnbreak % 145

  \item $\displaystyle f(x)=-5\left( x-\frac{2}{5}\right)^{2}+\frac{29}{5}$

$f(1)$
        \phantom{$=4$}

  \item $\displaystyle f(x)=3\left( x+\frac{5}{6}\right)^{2}-\frac{25}{12}$

$f(-1)$
        \phantom{$=-2$}

  \item $\displaystyle f(x)=4\left( x+\frac{1}{4}\right)^{2}-\frac{17}{4}$

$f(-4)$
        \phantom{$=52$}

  \item $\displaystyle f(x)=-4\left( x-\frac{5}{8}\right)^{2}+\frac{41}{16}$

$f(5)$
        \phantom{$=-74$}

  \item $\displaystyle f(x)=\left( x+2\right)^{2}$

$f(1)$
        \phantom{$=9$}

\columnbreak % 150

\resetques

  \item $\displaystyle f(x)=-4\left( x-\frac{1}{2}\right)^{2}-4$

$f(0)$
        \phantom{$=-5$}

  \item $\displaystyle f(x)=-4\left( x-\frac{3}{8}\right)^{2}-\frac{71}{16}$

$f(-2)$
        \phantom{$=-27$}

  \item $\displaystyle f(x)=-2\left( x-\frac{5}{4}\right)^{2}-\frac{7}{8}$

$f(1)$
        \phantom{$=-1$}

  \item $\displaystyle f(x)=\left( x-\frac{1}{2}\right)^{2}+\frac{7}{4}$

$f(-5)$
        \phantom{$=32$}

  \item $\displaystyle f(x)=-\left( x-\frac{3}{2}\right)^{2}-\frac{11}{4}$

$f(-2)$
        \phantom{$=-15$}

\columnbreak % 155

  \item $\displaystyle f(x)=-2\left( x+1\right)^{2}-2$

$f(1)$
        \phantom{$=-10$}

  \item $\displaystyle f(x)=\left( x+1\right)^{2}-4$

$f(3)$
        \phantom{$=12$}

  \item $\displaystyle f(x)=-\left( x-2\right)^{2}+5$

$f(1)$
        \phantom{$=4$}

  \item $\displaystyle f(x)=-\left( x-\frac{3}{2}\right)^{2}+\frac{25}{4}$

$f(-2)$
        \phantom{$=-6$}

  \item $\displaystyle f(x)=-\left( x-\frac{1}{2}\right)^{2}-\frac{3}{4}$

$f(-1)$
        \phantom{$=-3$}

\columnbreak % 160

\resetques

  \item $\displaystyle f(x)=-5\left( x+\frac{2}{5}\right)^{2}+\frac{9}{5}$

$f(-4)$
        \phantom{$=-63$}

  \item $\displaystyle f(x)=-3\left( x+\frac{1}{2}\right)^{2}+\frac{7}{4}$

$f(5)$
        \phantom{$=-89$}

  \item $\displaystyle f(x)=5\left( x-\frac{1}{2}\right)^{2}-\frac{9}{4}$

$f(-2)$
        \phantom{$=29$}

  \item $\displaystyle f(x)=-5\left( x-\frac{1}{10}\right)^{2}+\frac{41}{20}$

$f(2)$
        \phantom{$=-16$}

  \item $\displaystyle f(x)=-2\left( x-\frac{1}{2}\right)^{2}+\frac{3}{2}$

$f(2)$
        \phantom{$=-3$}

\columnbreak % 165

  \item $\displaystyle f(x)=3\left( x+\frac{1}{2}\right)^{2}-\frac{7}{4}$

$f(-4)$
        \phantom{$=35$}

  \item $\displaystyle f(x)=\left( x-\frac{3}{2}\right)^{2}-\frac{25}{4}$

$f(0)$
        \phantom{$=-4$}

  \item $\displaystyle f(x)=3\left( x+\frac{1}{3}\right)^{2}+\frac{5}{3}$

$f(5)$
        \phantom{$=87$}

  \item $\displaystyle f(x)=5\left( x-\frac{2}{5}\right)^{2}+\frac{1}{5}$

$f(-5)$
        \phantom{$=146$}

  \item $\displaystyle f(x)=2\left( x+\frac{1}{2}\right)^{2}+\frac{1}{2}$

$f(3)$
        \phantom{$=25$}

\columnbreak % 170

\resetques

  \item $\displaystyle f(x)=-\left( x+\frac{1}{2}\right)^{2}-\frac{15}{4}$

$f(-2)$
        \phantom{$=-6$}

  \item $\displaystyle f(x)=\left( x-1\right)^{2}+2$

$f(1)$
        \phantom{$=2$}

  \item $\displaystyle f(x)=4\left( x+\frac{1}{4}\right)^{2}+\frac{11}{4}$

$f(-4)$
        \phantom{$=59$}

  \item $\displaystyle f(x)=2\left( x+\frac{5}{4}\right)^{2}-\frac{25}{8}$

$f(-3)$
        \phantom{$=3$}

  \item $\displaystyle f(x)=-5\left( x-\frac{1}{5}\right)^{2}+\frac{16}{5}$

$f(4)$
        \phantom{$=-69$}

\columnbreak % 175

  \item $\displaystyle f(x)=3\left( x-\frac{5}{6}\right)^{2}+\frac{23}{12}$

$f(-1)$
        \phantom{$=12$}

  \item $\displaystyle f(x)=-3\left( x+\frac{5}{6}\right)^{2}+\frac{25}{12}$

$f(-4)$
        \phantom{$=-28$}

  \item $\displaystyle f(x)=-4\left( x+\frac{5}{8}\right)^{2}+\frac{25}{16}$

$f(2)$
        \phantom{$=-26$}

  \item $\displaystyle f(x)=-3\left( x-\frac{2}{3}\right)^{2}+\frac{19}{3}$

$f(3)$
        \phantom{$=-10$}

  \item $\displaystyle f(x)=-2\left( x+1\right)^{2}+2$

$f(-3)$
        \phantom{$=-6$}

\columnbreak % 180

\resetques

  \item $\displaystyle f(x)=-\left( x-1\right)^{2}+4$

$f(0)$
        \phantom{$=3$}

  \item $\displaystyle f(x)=5\left( x+\frac{1}{2}\right)^{2}-\frac{13}{4}$

$f(-4)$
        \phantom{$=58$}

  \item $\displaystyle f(x)=\left( x-\frac{1}{2}\right)^{2}+\frac{15}{4}$

$f(3)$
        \phantom{$=10$}

  \item $\displaystyle f(x)=-3\left( x-\frac{1}{3}\right)^{2}-\frac{11}{3}$

$f(0)$
        \phantom{$=-4$}

  \item $\displaystyle f(x)=-2\left( x+\frac{5}{4}\right)^{2}-\frac{7}{8}$

$f(-3)$
        \phantom{$=-7$}

\columnbreak % 185

  \item $\displaystyle f(x)=-2\left( x+1\right)^{2}+4$

$f(-1)$
        \phantom{$=4$}

  \item $\displaystyle f(x)=5\left( x-\frac{2}{5}\right)^{2}-\frac{24}{5}$

$f(0)$
        \phantom{$=-4$}

  \item $\displaystyle f(x)=4\left( x+\frac{5}{8}\right)^{2}+\frac{39}{16}$

$f(-3)$
        \phantom{$=25$}

  \item $\displaystyle f(x)=5\left( x-\frac{1}{5}\right)^{2}+\frac{19}{5}$

$f(-5)$
        \phantom{$=139$}

  \item $\displaystyle f(x)=-2\left( x+\frac{3}{4}\right)^{2}-\frac{23}{8}$

$f(-4)$
        \phantom{$=-24$}

\columnbreak % 190

\resetques

  \item $\displaystyle f(x)=3\left( x+\frac{5}{6}\right)^{2}-\frac{13}{12}$

$f(-4)$
        \phantom{$=29$}

  \item $\displaystyle f(x)=3\left( x-\frac{1}{3}\right)^{2}+\frac{14}{3}$

$f(0)$
        \phantom{$=5$}

  \item $\displaystyle f(x)=4\left( x+\frac{1}{4}\right)^{2}-\frac{13}{4}$

$f(3)$
        \phantom{$=39$}

  \item $\displaystyle f(x)=2\left( x-\frac{5}{4}\right)^{2}-\frac{57}{8}$

$f(-2)$
        \phantom{$=14$}

  \item $\displaystyle f(x)=5\left( x-\frac{1}{2}\right)^{2}+\frac{15}{4}$

$f(3)$
        \phantom{$=35$}

\columnbreak % 195

  \item $\displaystyle f(x)=-2\left( x-\frac{1}{2}\right)^{2}+\frac{1}{2}$

$f(5)$
        \phantom{$=-40$}

  \item $\displaystyle f(x)=2\left( x-\frac{1}{4}\right)^{2}+\frac{7}{8}$

$f(0)$
        \phantom{$=1$}

  \item $\displaystyle f(x)=3\left( x-\frac{1}{2}\right)^{2}-\frac{23}{4}$

$f(-2)$
        \phantom{$=13$}

  \item $\displaystyle f(x)=\left( x-\frac{3}{2}\right)^{2}-\frac{29}{4}$

$f(4)$
        \phantom{$=-1$}

  \item $\displaystyle f(x)=4\left( x-\frac{3}{8}\right)^{2}-\frac{89}{16}$

$f(2)$
        \phantom{$=5$}

\end{enumerate}
\end{multicols}

\newpage
\resetdrill

\begin{multicols}{2}
\begin{enumerate}
  \itemsep=90pt
  \item $\displaystyle f(x)=\left( x+2\right)^{2}-9$

$f(4)$
        $=27$

  \item $\displaystyle f(x)=-2\left( x-\frac{5}{4}\right)^{2}-\frac{15}{8}$

$f(-5)$
        $=-80$

  \item $\displaystyle f(x)=-5\left( x-\frac{1}{5}\right)^{2}-\frac{9}{5}$

$f(-2)$
        $=-26$

  \item $\displaystyle f(x)=4\left( x-\frac{3}{8}\right)^{2}-\frac{9}{16}$

$f(-3)$
        $=45$

  \item $\displaystyle f(x)=-5\left( x+\frac{1}{2}\right)^{2}-\frac{15}{4}$

$f(5)$
        $=-155$

\columnbreak % 5

  \item $\displaystyle f(x)=-5\left( x+\frac{1}{2}\right)^{2}+\frac{9}{4}$

$f(-3)$
        $=-29$

  \item $\displaystyle f(x)=-3\left( x-\frac{2}{3}\right)^{2}+\frac{1}{3}$

$f(3)$
        $=-16$

  \item $\displaystyle f(x)=\left( x+2\right)^{2}-1$

$f(1)$
        $=8$

  \item $\displaystyle f(x)=3\left( x+\frac{1}{2}\right)^{2}+\frac{13}{4}$

$f(4)$
        $=64$

  \item $\displaystyle f(x)=-2\left( x-\frac{3}{4}\right)^{2}+\frac{9}{8}$

$f(3)$
        $=-9$

\columnbreak % 10

\resetques

  \item $\displaystyle f(x)=\left( x-\frac{3}{2}\right)^{2}+\frac{3}{4}$

$f(2)$
        $=1$

  \item $\displaystyle f(x)=4\left( x+\frac{1}{2}\right)^{2}+4$

$f(4)$
        $=85$

  \item $\displaystyle f(x)=-3\left( x+\frac{2}{3}\right)^{2}+\frac{4}{3}$

$f(-2)$
        $=-4$

  \item $\displaystyle f(x)=-4\left( x-\frac{1}{2}\right)^{2}+6$

$f(-5)$
        $=-115$

  \item $\displaystyle f(x)=-3\left( x-\frac{1}{2}\right)^{2}+\frac{11}{4}$

$f(0)$
        $=2$

\columnbreak % 15

  \item $\displaystyle f(x)=\left( x+\frac{1}{2}\right)^{2}-\frac{21}{4}$

$f(-2)$
        $=-3$

  \item $\displaystyle f(x)=\left( x-\frac{1}{2}\right)^{2}-\frac{17}{4}$

$f(-1)$
        $=-2$

  \item $\displaystyle f(x)=-\left( x+2\right)^{2}+7$

$f(4)$
        $=-29$

  \item $\displaystyle f(x)=3\left( x-\frac{1}{3}\right)^{2}+\frac{5}{3}$

$f(0)$
        $=2$

  \item $\displaystyle f(x)=2\left( x-1\right)^{2}-1$

$f(-5)$
        $=71$

\columnbreak % 20

\resetques

  \item $\displaystyle f(x)=3\left( x+\frac{1}{3}\right)^{2}-\frac{10}{3}$

$f(-3)$
        $=18$

  \item $\displaystyle f(x)=5\left( x-\frac{1}{2}\right)^{2}+\frac{7}{4}$

$f(-5)$
        $=153$

  \item $\displaystyle f(x)=-\left( x-\frac{5}{2}\right)^{2}+\frac{37}{4}$

$f(1)$
        $=7$

  \item $\displaystyle f(x)=2\left( x+\frac{1}{4}\right)^{2}+\frac{23}{8}$

$f(3)$
        $=24$

  \item $\displaystyle f(x)=-2\left( x-\frac{5}{4}\right)^{2}-\frac{7}{8}$

$f(2)$
        $=-2$

\columnbreak % 25

  \item $\displaystyle f(x)=-5\left( x+\frac{1}{10}\right)^{2}-\frac{99}{20}$

$f(-2)$
        $=-23$

  \item $\displaystyle f(x)=\left( x-1\right)^{2}+3$

$f(-3)$
        $=19$

  \item $\displaystyle f(x)=-2\left( x+\frac{3}{4}\right)^{2}+\frac{9}{8}$

$f(1)$
        $=-5$

  \item $\displaystyle f(x)=-4\left( x+\frac{1}{2}\right)^{2}-3$

$f(2)$
        $=-28$

  \item $\displaystyle f(x)=3\left( x-\frac{2}{3}\right)^{2}-\frac{10}{3}$

$f(4)$
        $=30$

\columnbreak % 30

\resetques

  \item $\displaystyle f(x)=-4\left( x+\frac{3}{8}\right)^{2}-\frac{71}{16}$

$f(-3)$
        $=-32$

  \item $\displaystyle f(x)=5\left( x-\frac{2}{5}\right)^{2}-\frac{19}{5}$

$f(-2)$
        $=25$

  \item $\displaystyle f(x)=-3\left( x-\frac{1}{3}\right)^{2}+\frac{7}{3}$

$f(-1)$
        $=-3$

  \item $\displaystyle f(x)=-\left( x-1\right)^{2}$

$f(0)$
        $=-1$

  \item $\displaystyle f(x)=-4\left( x-\frac{1}{2}\right)^{2}+6$

$f(-4)$
        $=-75$

\columnbreak % 35

  \item $\displaystyle f(x)=\left( x-\frac{3}{2}\right)^{2}+\frac{11}{4}$

$f(-4)$
        $=33$

  \item $\displaystyle f(x)=3\left( x-\frac{5}{6}\right)^{2}+\frac{23}{12}$

$f(5)$
        $=54$

  \item $\displaystyle f(x)=\left( x+2\right)^{2}+1$

$f(3)$
        $=26$

  \item $\displaystyle f(x)=-4\left( x+\frac{1}{4}\right)^{2}-\frac{19}{4}$

$f(5)$
        $=-115$

  \item $\displaystyle f(x)=2\left( x-\frac{5}{4}\right)^{2}-\frac{49}{8}$

$f(5)$
        $=22$

\columnbreak % 40

\resetques

  \item $\displaystyle f(x)=-3\left( x+\frac{5}{6}\right)^{2}-\frac{23}{12}$

$f(4)$
        $=-72$

  \item $\displaystyle f(x)=-4\left( x+\frac{1}{2}\right)^{2}+4$

$f(-1)$
        $=3$

  \item $\displaystyle f(x)=-2\left( x-\frac{1}{2}\right)^{2}-\frac{1}{2}$

$f(-4)$
        $=-41$

  \item $\displaystyle f(x)=\left( x-\frac{5}{2}\right)^{2}-\frac{13}{4}$

$f(3)$
        $=-3$

  \item $\displaystyle f(x)=5\left( x-\frac{2}{5}\right)^{2}-\frac{14}{5}$

$f(0)$
        $=-2$

\columnbreak % 45

  \item $\displaystyle f(x)=3\left( x+\frac{2}{3}\right)^{2}-\frac{1}{3}$

$f(5)$
        $=96$

  \item $\displaystyle f(x)=-3\left( x+\frac{5}{6}\right)^{2}+\frac{73}{12}$

$f(0)$
        $=4$

  \item $\displaystyle f(x)=2\left( x-1\right)^{2}+1$

$f(-3)$
        $=33$

  \item $\displaystyle f(x)=-5\left( x+\frac{1}{2}\right)^{2}+\frac{9}{4}$

$f(-4)$
        $=-59$

  \item $\displaystyle f(x)=-5\left( x+\frac{2}{5}\right)^{2}-\frac{11}{5}$

$f(0)$
        $=-3$

\columnbreak % 50

\resetques

  \item $\displaystyle f(x)=-3\left( x+\frac{1}{6}\right)^{2}-\frac{23}{12}$

$f(-1)$
        $=-4$

  \item $\displaystyle f(x)=-\left( x-\frac{5}{2}\right)^{2}+\frac{9}{4}$

$f(5)$
        $=-4$

  \item $\displaystyle f(x)=2\left( x-1\right)^{2}-7$

$f(-3)$
        $=25$

  \item $\displaystyle f(x)=4\left( x-\frac{1}{2}\right)^{2}+4$

$f(0)$
        $=5$

  \item $\displaystyle f(x)=-3\left( x-\frac{1}{6}\right)^{2}-\frac{35}{12}$

$f(2)$
        $=-13$

\columnbreak % 55

  \item $\displaystyle f(x)=4\left( x+\frac{1}{8}\right)^{2}+\frac{47}{16}$

$f(4)$
        $=71$

  \item $\displaystyle f(x)=-5\left( x-\frac{3}{10}\right)^{2}+\frac{69}{20}$

$f(-3)$
        $=-51$

  \item $\displaystyle f(x)=5\left( x-\frac{2}{5}\right)^{2}-\frac{19}{5}$

$f(-3)$
        $=54$

  \item $\displaystyle f(x)=-5\left( x-\frac{1}{10}\right)^{2}-\frac{39}{20}$

$f(-3)$
        $=-50$

  \item $\displaystyle f(x)=5\left( x+\frac{1}{2}\right)^{2}-\frac{9}{4}$

$f(4)$
        $=99$

\columnbreak % 60

\resetques

  \item $\displaystyle f(x)=-3\left( x-\frac{1}{2}\right)^{2}+\frac{3}{4}$

$f(-2)$
        $=-18$

  \item $\displaystyle f(x)=4\left( x+\frac{3}{8}\right)^{2}-\frac{89}{16}$

$f(4)$
        $=71$

  \item $\displaystyle f(x)=-4\left( x-\frac{1}{8}\right)^{2}+\frac{1}{16}$

$f(1)$
        $=-3$

  \item $\displaystyle f(x)=5\left( x+\frac{1}{2}\right)^{2}+\frac{15}{4}$

$f(-3)$
        $=35$

  \item $\displaystyle f(x)=5\left( x+\frac{1}{10}\right)^{2}-\frac{61}{20}$

$f(-3)$
        $=39$

\columnbreak % 65

  \item $\displaystyle f(x)=5\left( x+\frac{1}{10}\right)^{2}+\frac{59}{20}$

$f(5)$
        $=133$

  \item $\displaystyle f(x)=5\left( x+\frac{1}{5}\right)^{2}+\frac{14}{5}$

$f(2)$
        $=27$

  \item $\displaystyle f(x)=4\left( x+\frac{1}{4}\right)^{2}+\frac{3}{4}$

$f(2)$
        $=21$

  \item $\displaystyle f(x)=-2\left( x+\frac{1}{2}\right)^{2}+\frac{7}{2}$

$f(3)$
        $=-21$

  \item $\displaystyle f(x)=-5\left( x+\frac{2}{5}\right)^{2}+\frac{4}{5}$

$f(-3)$
        $=-33$

\columnbreak % 70

\resetques

  \item $\displaystyle f(x)=\left( x-1\right)^{2}+2$

$f(5)$
        $=18$

  \item $\displaystyle f(x)=-3\left( x+\frac{1}{2}\right)^{2}+\frac{3}{4}$

$f(-1)$
        $=0$

  \item $\displaystyle f(x)=3\left( x+\frac{2}{3}\right)^{2}+\frac{8}{3}$

$f(0)$
        $=4$

  \item $\displaystyle f(x)=3\left( x-\frac{2}{3}\right)^{2}-\frac{10}{3}$

$f(-3)$
        $=37$

  \item $\displaystyle f(x)=-4\left( x-\frac{1}{2}\right)^{2}+4$

$f(-4)$
        $=-77$

\columnbreak % 75

  \item $\displaystyle f(x)=-\left( x+1\right)^{2}+1$

$f(0)$
        $=0$

  \item $\displaystyle f(x)=-5\left( x-\frac{2}{5}\right)^{2}-\frac{1}{5}$

$f(4)$
        $=-65$

  \item $\displaystyle f(x)=4\left( x+\frac{1}{2}\right)^{2}$

$f(-4)$
        $=49$

  \item $\displaystyle f(x)=-3\left( x-\frac{1}{6}\right)^{2}+\frac{49}{12}$

$f(-2)$
        $=-10$

  \item $\displaystyle f(x)=2\left( x+\frac{1}{2}\right)^{2}-\frac{5}{2}$

$f(3)$
        $=22$

\columnbreak % 80

\resetques

  \item $\displaystyle f(x)=2\left( x+\frac{1}{4}\right)^{2}-\frac{41}{8}$

$f(4)$
        $=31$

  \item $\displaystyle f(x)=3\left( x+\frac{2}{3}\right)^{2}+\frac{8}{3}$

$f(4)$
        $=68$

  \item $\displaystyle f(x)=-5\left( x+\frac{2}{5}\right)^{2}+\frac{14}{5}$

$f(4)$
        $=-94$

  \item $\displaystyle f(x)=\left( x-1\right)^{2}+1$

$f(-5)$
        $=37$

  \item $\displaystyle f(x)=4\left( x+\frac{1}{4}\right)^{2}+\frac{3}{4}$

$f(1)$
        $=7$

\columnbreak % 85

  \item $\displaystyle f(x)=-3\left( x-\frac{5}{6}\right)^{2}-\frac{11}{12}$

$f(-1)$
        $=-11$

  \item $\displaystyle f(x)=-2\left( x+\frac{5}{4}\right)^{2}+\frac{25}{8}$

$f(-2)$
        $=2$

  \item $\displaystyle f(x)=-4\left( x+\frac{1}{8}\right)^{2}-\frac{63}{16}$

$f(2)$
        $=-22$

  \item $\displaystyle f(x)=-4\left( x+\frac{1}{8}\right)^{2}+\frac{17}{16}$

$f(-4)$
        $=-59$

  \item $\displaystyle f(x)=-4\left( x+\frac{1}{2}\right)^{2}-3$

$f(0)$
        $=-4$

\columnbreak % 90

\resetques

  \item $\displaystyle f(x)=-3\left( x+\frac{1}{2}\right)^{2}+\frac{7}{4}$

$f(-1)$
        $=1$

  \item $\displaystyle f(x)=-3\left( x+\frac{2}{3}\right)^{2}+\frac{10}{3}$

$f(0)$
        $=2$

  \item $\displaystyle f(x)=3\left( x-\frac{1}{2}\right)^{2}+\frac{1}{4}$

$f(-4)$
        $=61$

  \item $\displaystyle f(x)=-5\left( x-\frac{3}{10}\right)^{2}+\frac{109}{20}$

$f(2)$
        $=-9$

  \item $\displaystyle f(x)=-3\left( x+\frac{5}{6}\right)^{2}+\frac{61}{12}$

$f(-1)$
        $=5$

\columnbreak % 95

  \item $\displaystyle f(x)=-4\left( x-\frac{3}{8}\right)^{2}-\frac{55}{16}$

$f(-2)$
        $=-26$

  \item $\displaystyle f(x)=\left( x-2\right)^{2}-1$

$f(-3)$
        $=24$

  \item $\displaystyle f(x)=\left( x-\frac{3}{2}\right)^{2}-\frac{13}{4}$

$f(-1)$
        $=3$

  \item $\displaystyle f(x)=5\left( x+\frac{1}{10}\right)^{2}+\frac{59}{20}$

$f(3)$
        $=51$

  \item $\displaystyle f(x)=5\left( x+\frac{1}{2}\right)^{2}-\frac{9}{4}$

$f(-4)$
        $=59$

\columnbreak % 100

\resetques

  \item $\displaystyle f(x)=4\left( x+\frac{5}{8}\right)^{2}-\frac{89}{16}$

$f(4)$
        $=80$

  \item $\displaystyle f(x)=-2\left( x-\frac{5}{4}\right)^{2}-\frac{7}{8}$

$f(-3)$
        $=-37$

  \item $\displaystyle f(x)=-5\left( x-\frac{1}{2}\right)^{2}-\frac{3}{4}$

$f(5)$
        $=-102$

  \item $\displaystyle f(x)=3\left( x+\frac{1}{6}\right)^{2}-\frac{25}{12}$

$f(-3)$
        $=22$

  \item $\displaystyle f(x)=3\left( x+\frac{2}{3}\right)^{2}-\frac{4}{3}$

$f(5)$
        $=95$

\columnbreak % 105

  \item $\displaystyle f(x)=5\left( x-\frac{1}{10}\right)^{2}-\frac{61}{20}$

$f(-5)$
        $=127$

  \item $\displaystyle f(x)=-2\left( x-1\right)^{2}-2$

$f(0)$
        $=-4$

  \item $\displaystyle f(x)=3\left( x+\frac{1}{6}\right)^{2}+\frac{23}{12}$

$f(5)$
        $=82$

  \item $\displaystyle f(x)=-2\left( x+\frac{5}{4}\right)^{2}+\frac{65}{8}$

$f(-1)$
        $=8$

  \item $\displaystyle f(x)=-5\left( x+\frac{3}{10}\right)^{2}+\frac{69}{20}$

$f(-2)$
        $=-11$

\columnbreak % 110

\resetques

  \item $\displaystyle f(x)=\left( x+\frac{3}{2}\right)^{2}-\frac{9}{4}$

$f(5)$
        $=40$

  \item $\displaystyle f(x)=-2\left( x+\frac{3}{4}\right)^{2}+\frac{33}{8}$

$f(-4)$
        $=-17$

  \item $\displaystyle f(x)=-\left( x+\frac{3}{2}\right)^{2}+\frac{1}{4}$

$f(-4)$
        $=-6$

  \item $\displaystyle f(x)=-5\left( x+\frac{2}{5}\right)^{2}-\frac{16}{5}$

$f(5)$
        $=-149$

  \item $\displaystyle f(x)=-2\left( x-\frac{1}{2}\right)^{2}+\frac{9}{2}$

$f(-3)$
        $=-20$

\columnbreak % 115

  \item $\displaystyle f(x)=-3\left( x-\frac{1}{3}\right)^{2}-\frac{5}{3}$

$f(1)$
        $=-3$

  \item $\displaystyle f(x)=5\left( x+\frac{1}{5}\right)^{2}+\frac{4}{5}$

$f(4)$
        $=89$

  \item $\displaystyle f(x)=2\left( x+\frac{1}{4}\right)^{2}+\frac{31}{8}$

$f(-3)$
        $=19$

  \item $\displaystyle f(x)=-2\left( x+1\right)^{2}+5$

$f(5)$
        $=-67$

  \item $\displaystyle f(x)=-4\left( x+\frac{1}{4}\right)^{2}-\frac{19}{4}$

$f(2)$
        $=-25$

\columnbreak % 120

\resetques

  \item $\displaystyle f(x)=\left( x+2\right)^{2}-3$

$f(2)$
        $=13$

  \item $\displaystyle f(x)=3\left( x+\frac{5}{6}\right)^{2}-\frac{37}{12}$

$f(5)$
        $=99$

  \item $\displaystyle f(x)=-2\left( x+\frac{5}{4}\right)^{2}+\frac{65}{8}$

$f(-1)$
        $=8$

  \item $\displaystyle f(x)=2\left( x+\frac{1}{2}\right)^{2}-\frac{1}{2}$

$f(3)$
        $=24$

  \item $\displaystyle f(x)=3\left( x+\frac{5}{6}\right)^{2}-\frac{85}{12}$

$f(-5)$
        $=45$

\columnbreak % 125

  \item $\displaystyle f(x)=3\left( x+\frac{1}{2}\right)^{2}+\frac{9}{4}$

$f(1)$
        $=9$

  \item $\displaystyle f(x)=2\left( x+1\right)^{2}-6$

$f(3)$
        $=26$

  \item $\displaystyle f(x)=-4\left( x-\frac{3}{8}\right)^{2}+\frac{73}{16}$

$f(5)$
        $=-81$

  \item $\displaystyle f(x)=-2\left( x+\frac{1}{4}\right)^{2}-\frac{7}{8}$

$f(-4)$
        $=-29$

  \item $\displaystyle f(x)=5\left( x-\frac{1}{2}\right)^{2}-\frac{17}{4}$

$f(5)$
        $=97$

\columnbreak % 130

\resetques

  \item $\displaystyle f(x)=4\left( x+\frac{3}{8}\right)^{2}-\frac{89}{16}$

$f(1)$
        $=2$

  \item $\displaystyle f(x)=-\left( x+1\right)^{2}+1$

$f(-3)$
        $=-3$

  \item $\displaystyle f(x)=-5\left( x-\frac{1}{5}\right)^{2}+\frac{21}{5}$

$f(-3)$
        $=-47$

  \item $\displaystyle f(x)=2\left( x-1\right)^{2}-4$

$f(-3)$
        $=28$

  \item $\displaystyle f(x)=-2\left( x+\frac{1}{4}\right)^{2}-\frac{7}{8}$

$f(-2)$
        $=-7$

\columnbreak % 135

  \item $\displaystyle f(x)=-3\left( x-\frac{5}{6}\right)^{2}+\frac{73}{12}$

$f(-3)$
        $=-38$

  \item $\displaystyle f(x)=-3\left( x-\frac{5}{6}\right)^{2}+\frac{25}{12}$

$f(4)$
        $=-28$

  \item $\displaystyle f(x)=3\left( x+\frac{1}{3}\right)^{2}+\frac{14}{3}$

$f(4)$
        $=61$

  \item $\displaystyle f(x)=-5\left( x-\frac{1}{5}\right)^{2}-\frac{19}{5}$

$f(5)$
        $=-119$

  \item $\displaystyle f(x)=4\left( x+\frac{1}{4}\right)^{2}-\frac{9}{4}$

$f(5)$
        $=108$

\columnbreak % 140

\resetques

  \item $\displaystyle f(x)=\left( x-\frac{1}{2}\right)^{2}+\frac{19}{4}$

$f(0)$
        $=5$

  \item $\displaystyle f(x)=\left( x+\frac{1}{2}\right)^{2}-\frac{1}{4}$

$f(-5)$
        $=20$

  \item $\displaystyle f(x)=3\left( x+\frac{2}{3}\right)^{2}+\frac{5}{3}$

$f(1)$
        $=10$

  \item $\displaystyle f(x)=4\left( x+\frac{1}{2}\right)^{2}-2$

$f(-4)$
        $=47$

  \item $\displaystyle f(x)=4\left( x-\frac{5}{8}\right)^{2}-\frac{41}{16}$

$f(-2)$
        $=25$

\columnbreak % 145

  \item $\displaystyle f(x)=-5\left( x-\frac{2}{5}\right)^{2}+\frac{29}{5}$

$f(1)$
        $=4$

  \item $\displaystyle f(x)=3\left( x+\frac{5}{6}\right)^{2}-\frac{25}{12}$

$f(-1)$
        $=-2$

  \item $\displaystyle f(x)=4\left( x+\frac{1}{4}\right)^{2}-\frac{17}{4}$

$f(-4)$
        $=52$

  \item $\displaystyle f(x)=-4\left( x-\frac{5}{8}\right)^{2}+\frac{41}{16}$

$f(5)$
        $=-74$

  \item $\displaystyle f(x)=\left( x+2\right)^{2}$

$f(1)$
        $=9$

\columnbreak % 150

\resetques

  \item $\displaystyle f(x)=-4\left( x-\frac{1}{2}\right)^{2}-4$

$f(0)$
        $=-5$

  \item $\displaystyle f(x)=-4\left( x-\frac{3}{8}\right)^{2}-\frac{71}{16}$

$f(-2)$
        $=-27$

  \item $\displaystyle f(x)=-2\left( x-\frac{5}{4}\right)^{2}-\frac{7}{8}$

$f(1)$
        $=-1$

  \item $\displaystyle f(x)=\left( x-\frac{1}{2}\right)^{2}+\frac{7}{4}$

$f(-5)$
        $=32$

  \item $\displaystyle f(x)=-\left( x-\frac{3}{2}\right)^{2}-\frac{11}{4}$

$f(-2)$
        $=-15$

\columnbreak % 155

  \item $\displaystyle f(x)=-2\left( x+1\right)^{2}-2$

$f(1)$
        $=-10$

  \item $\displaystyle f(x)=\left( x+1\right)^{2}-4$

$f(3)$
        $=12$

  \item $\displaystyle f(x)=-\left( x-2\right)^{2}+5$

$f(1)$
        $=4$

  \item $\displaystyle f(x)=-\left( x-\frac{3}{2}\right)^{2}+\frac{25}{4}$

$f(-2)$
        $=-6$

  \item $\displaystyle f(x)=-\left( x-\frac{1}{2}\right)^{2}-\frac{3}{4}$

$f(-1)$
        $=-3$

\columnbreak % 160

\resetques

  \item $\displaystyle f(x)=-5\left( x+\frac{2}{5}\right)^{2}+\frac{9}{5}$

$f(-4)$
        $=-63$

  \item $\displaystyle f(x)=-3\left( x+\frac{1}{2}\right)^{2}+\frac{7}{4}$

$f(5)$
        $=-89$

  \item $\displaystyle f(x)=5\left( x-\frac{1}{2}\right)^{2}-\frac{9}{4}$

$f(-2)$
        $=29$

  \item $\displaystyle f(x)=-5\left( x-\frac{1}{10}\right)^{2}+\frac{41}{20}$

$f(2)$
        $=-16$

  \item $\displaystyle f(x)=-2\left( x-\frac{1}{2}\right)^{2}+\frac{3}{2}$

$f(2)$
        $=-3$

\columnbreak % 165

  \item $\displaystyle f(x)=3\left( x+\frac{1}{2}\right)^{2}-\frac{7}{4}$

$f(-4)$
        $=35$

  \item $\displaystyle f(x)=\left( x-\frac{3}{2}\right)^{2}-\frac{25}{4}$

$f(0)$
        $=-4$

  \item $\displaystyle f(x)=3\left( x+\frac{1}{3}\right)^{2}+\frac{5}{3}$

$f(5)$
        $=87$

  \item $\displaystyle f(x)=5\left( x-\frac{2}{5}\right)^{2}+\frac{1}{5}$

$f(-5)$
        $=146$

  \item $\displaystyle f(x)=2\left( x+\frac{1}{2}\right)^{2}+\frac{1}{2}$

$f(3)$
        $=25$

\columnbreak % 170

\resetques

  \item $\displaystyle f(x)=-\left( x+\frac{1}{2}\right)^{2}-\frac{15}{4}$

$f(-2)$
        $=-6$

  \item $\displaystyle f(x)=\left( x-1\right)^{2}+2$

$f(1)$
        $=2$

  \item $\displaystyle f(x)=4\left( x+\frac{1}{4}\right)^{2}+\frac{11}{4}$

$f(-4)$
        $=59$

  \item $\displaystyle f(x)=2\left( x+\frac{5}{4}\right)^{2}-\frac{25}{8}$

$f(-3)$
        $=3$

  \item $\displaystyle f(x)=-5\left( x-\frac{1}{5}\right)^{2}+\frac{16}{5}$

$f(4)$
        $=-69$

\columnbreak % 175

  \item $\displaystyle f(x)=3\left( x-\frac{5}{6}\right)^{2}+\frac{23}{12}$

$f(-1)$
        $=12$

  \item $\displaystyle f(x)=-3\left( x+\frac{5}{6}\right)^{2}+\frac{25}{12}$

$f(-4)$
        $=-28$

  \item $\displaystyle f(x)=-4\left( x+\frac{5}{8}\right)^{2}+\frac{25}{16}$

$f(2)$
        $=-26$

  \item $\displaystyle f(x)=-3\left( x-\frac{2}{3}\right)^{2}+\frac{19}{3}$

$f(3)$
        $=-10$

  \item $\displaystyle f(x)=-2\left( x+1\right)^{2}+2$

$f(-3)$
        $=-6$

\columnbreak % 180

\resetques

  \item $\displaystyle f(x)=-\left( x-1\right)^{2}+4$

$f(0)$
        $=3$

  \item $\displaystyle f(x)=5\left( x+\frac{1}{2}\right)^{2}-\frac{13}{4}$

$f(-4)$
        $=58$

  \item $\displaystyle f(x)=\left( x-\frac{1}{2}\right)^{2}+\frac{15}{4}$

$f(3)$
        $=10$

  \item $\displaystyle f(x)=-3\left( x-\frac{1}{3}\right)^{2}-\frac{11}{3}$

$f(0)$
        $=-4$

  \item $\displaystyle f(x)=-2\left( x+\frac{5}{4}\right)^{2}-\frac{7}{8}$

$f(-3)$
        $=-7$

\columnbreak % 185

  \item $\displaystyle f(x)=-2\left( x+1\right)^{2}+4$

$f(-1)$
        $=4$

  \item $\displaystyle f(x)=5\left( x-\frac{2}{5}\right)^{2}-\frac{24}{5}$

$f(0)$
        $=-4$

  \item $\displaystyle f(x)=4\left( x+\frac{5}{8}\right)^{2}+\frac{39}{16}$

$f(-3)$
        $=25$

  \item $\displaystyle f(x)=5\left( x-\frac{1}{5}\right)^{2}+\frac{19}{5}$

$f(-5)$
        $=139$

  \item $\displaystyle f(x)=-2\left( x+\frac{3}{4}\right)^{2}-\frac{23}{8}$

$f(-4)$
        $=-24$

\columnbreak % 190

\resetques

  \item $\displaystyle f(x)=3\left( x+\frac{5}{6}\right)^{2}-\frac{13}{12}$

$f(-4)$
        $=29$

  \item $\displaystyle f(x)=3\left( x-\frac{1}{3}\right)^{2}+\frac{14}{3}$

$f(0)$
        $=5$

  \item $\displaystyle f(x)=4\left( x+\frac{1}{4}\right)^{2}-\frac{13}{4}$

$f(3)$
        $=39$

  \item $\displaystyle f(x)=2\left( x-\frac{5}{4}\right)^{2}-\frac{57}{8}$

$f(-2)$
        $=14$

  \item $\displaystyle f(x)=5\left( x-\frac{1}{2}\right)^{2}+\frac{15}{4}$

$f(3)$
        $=35$

\columnbreak % 195

  \item $\displaystyle f(x)=-2\left( x-\frac{1}{2}\right)^{2}+\frac{1}{2}$

$f(5)$
        $=-40$

  \item $\displaystyle f(x)=2\left( x-\frac{1}{4}\right)^{2}+\frac{7}{8}$

$f(0)$
        $=1$

  \item $\displaystyle f(x)=3\left( x-\frac{1}{2}\right)^{2}-\frac{23}{4}$

$f(-2)$
        $=13$

  \item $\displaystyle f(x)=\left( x-\frac{3}{2}\right)^{2}-\frac{29}{4}$

$f(4)$
        $=-1$

  \item $\displaystyle f(x)=4\left( x-\frac{3}{8}\right)^{2}-\frac{89}{16}$

$f(2)$
        $=5$

\end{enumerate}
\end{multicols}
